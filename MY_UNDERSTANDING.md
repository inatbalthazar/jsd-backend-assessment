# My Understanding

**AI Rating ของฉัน:** 5
---

## Backend

**1. HTTP method แต่ละตัวในแอปของคุณหมายถึงอะไร — GET, POST, PUT or PATCH, และ DELETE? ทำไมเราถึงใช้ method ต่างกัน แทนที่จะใช้ POST สำหรับทุกอย่าง?**

ความหมายของ HTTP Methods GET: ใช้ดึงข้อมูลจาก Server แบบไม่มีการเปลี่ยนแปลงของข้อมูลเลย

POST: ใช้ส่งข้อมูลใหม่ไปให้ Server เพื่อทำการสร้างข้อมูล เช่น register หรือ login

PUT: ใช้แทนที่ข้อมูลเดิมทั้งหมด ส่วนข้อมูลเก่าจะโดนทับทันที ถึงจะเปลี่ยนแปลงแค่ส่วนเดียว ส่วนที่เหลือจะหายหมดและแทนที่ด้วยข้อมูลใหม่

PATCH: ใช้แก้ไขข้อมูลเดิมเฉพาะที่หรือส่วนที่ต้องการ

DELETE: ใช้ลบข้อมูลที่ระบุออกจาก Server

แม้ว่าในทางเทคนิคเราจะสามารถใช้ POST ทำทุกอย่างได้ เพราะส่งข้อมูลได้เหมือนกัน แต่การทำแบบนั้นจะส่งผลเสียร้ายแรงต่อระบบ เช่น ความปลอดภัย หากเราอยากเอาข้อมูลออกมาโชว์ การใช้ GET จะปลอดภัยกว่าเพราะเป็น Safe Method ดูได้อย่างเดียว แก้ข้อมูลไม่ได้ แต่ถ้าเราใช้ POST กับทุกสิ่งทุกอย่าง ระบบ Firewall หรือ API Gateway จะแยกแยะไม่ได้เลยว่า POST ไหนแค่เข้ามาดูข้อมูล หรือ POST ไหนส่งเข้ามาเพื่อขอลบฐานข้อมูล ทำให้ระบบเสี่ยงต่อการโดนโจมตีได้ง่ายขึ้นมาก

Caching GET สามารถทำ Cache ได้ ทำให้เว็บโหลดเร็วและลดการทำงานของ Server แต่ถ้าใช้ POST ดึงข้อมูล ระบบจะไม่ทำ Cache ทำให้เว็บช้าและ Server ทำงานหนักเกินไป

PUT และ DELETE ส่งซ้ำได้ไม่สร้างข้อมูลซ้ำซ้อน แต่ถ้าใช้ POST กับทุกอย่าง แล้วเกิดเน็ตหลุดจนระบบต้องส่งคำสั่งซ้ำ จะทำให้เกิดข้อมูลขยะหรือข้อมูลซ้ำซ้อนในระบบทันที

---

**2. `express.json()` คืออะไร และจะเกิดอะไรขึ้นถ้าคุณไม่ใส่มัน?**

express.json() มันมีหน้าที่คอยตรวจจับ Request ที่ส่งข้อมูลมาเป็นรูปแบบ JSON จากนั้นมันจะทำการ แปลง ข้อความ JSON ยาวๆ ให้เป็น JavaScript Object แล้วเอาไปแปะไว้ที่ตัวแปร req.body เพื่อให้เรานำข้อมูลไปใช้งานต่อ การทำงานจริงๆถ้าให้พูดถึงการทำงาน user ยิงreq มากัล header express.json() จะดักจับและมาแปลงให้เป็น JavaScript Object แปะเข้า req.body หลังจากนะเราก็เอามา แกะ obj อีกทีครับ

ถ้าไม่ใส่ req.body จะกลายเป็น undefined sever จะมองเห็นข้อมูลที่ส่งมาเป็นแค่ข้อความดิบ ที่อ่านไม่ออก

ระบบจะพัง Bug เมื่อเราพยายามดึงข้อมูลมาใช้งาน เช่น req.body.username แต่ในเมื่อ req.body มันว่างเปล่า โปรแกรมจะเกิด Runtime Error ทันที

---

**3. `req.body`, `req.params`, และ `req.query` ต่างกันอย่างไร? ยกตัวอย่างจริงจาก API ของคุณสำหรับแต่ละตัว**

req.params ใช้เมื่อต้องการเข้าถึงสินค้าชิ้นใดชิ้นหนึ่งโดยเฉพาะ ผ่าน ID ของสินค้านั้น ดึงข้อมูลจาก URL Path มักเป็น ID หรือชื่อเฉพาะที่ถูกล็อกตำแหน่งไว้

req.query ดึงข้อมูลจาก URL Query String อยู่หลังเครื่องหมาย ? ใช้ในการค้นหา กรองข้อมูล หรือแบ่งหน้า

req.body ดึงข้อมูลจาก Request Body ข้อมูลก้อนใหญ่ที่ซ่อนมากับ Request มักเป็นฟอร์มข้อมูลเพื่อใช้สร้างหรือแก้ไข

---

**4. HTTP status codes คืออะไร? ระบุรายการ status code ทุกตัวที่คุณใช้ใน API และอธิบายว่าทำไมถึงเลือกใช้ในแต่ละสถานการณ์**

HTTP Status Codes รหัสสถานะ HTTP คือ รหัสตัวเลขมาตรฐาน 3 หลัก ที่เซิร์ฟเวอร์ส่งกลับไปให้ฝั่งผู้ใช้ เช่น Web Browser หรือแอปพลิเคชัน เพื่อเป็นภาษากลางบอกให้รู้ทันทีว่า คำขอ Request ที่ส่งมา ทำงานสำเร็จ หรือเกิดปัญหาอะไรขึ้น

Status Codes 200 OK สำเร็จ ส่วนมาจะเป็ฯการดึงข้อมูลออกมาโชว์ ถ้าสำเร็จจะขึ้น 200

201 สร้างข้อมูลใหม่สำเร็จ 201 จะเจาะจงและถูกต้องที่สุดสำหรับคำสั่ง POST ที่ทำให้เกิดการสร้างทรัพยากรใหม่

400 Bad Request ข้อมูลที่ส่งมาไม่ถูกต้อง ส่วนมากจะเป็น logic ที่เราเขียนดักไว้ ถ้าใส่ข้อมูลไม่ครบ จะขึ้น 400

404 Not Found หาไม่เจอ หรือไม่มีข้อมูลอยู่จริงในระบบ

ทุกอย่างอยู่ที่เรากำหนด และเป็นวิธีที่ดีที่สุด เพราะถ้าเกิดปัญหาเราจะรู้ทันทีว่าตรงไหนเป็นอะไร

---

**5. middleware คืออะไร? อธิบายด้วยคำพูดของคุณเองว่ามันทำอะไร พร้อมยกตัวอย่าง 1 อย่างจากโค้ดของคุณ**

ถ้าพูดให้เห็นภาพง่ายๆ Middleware เปรียบเสมือน ด่านตรวจ ที่คอยยืนดักอยู่ตรงกลางระหว่างที่ Request ถูกส่งมาจากหน้าบ้าน ก่อนที่มันจะวิ่งไปถึงโค้ดประมวลผลหลัก ที่คอยคัดกรอง ตรวจสอบ หรือแต่งเติมข้อมูลไปเรื่อยๆ ตามลำดับ

---

**6. ทำไม order ของ middleware ใน Express ถึงสำคัญ? จะเกิดอะไรขึ้นถ้า order ผิด?**

ทำไมลำดับของ Middleware ถึงสำคัญ ใน Express.js ระบบจะประมวลผล Middleware เรียงจาก บนลงล่าง Express ทำงานเหมือน "สายพานในโรงงาน" ที่ Request จะวิ่งผ่านด่านต่างๆ ทีละด่าน โดยด่านก่อนหน้าจะต้องทำหน้าที่ตรวจเช็กหรือปรับแต่งข้อมูลให้เสร็จ แล้วสั่ง next() เพื่อส่งต่อให้ด่านถัดไป หากเราสลับลำดับด่านผิด ระบบจะพังทันทีครับ

---

**7. อธิบายทีละขั้นตอนว่าเกิดอะไรขึ้นบน server เมื่อมี POST request ถูกส่งไปที่ `/products`**

Client ยิง post หรือกำลังสร้างข้อมูล POST /products
รับคำขอเข้าสู่เซิร์ฟเวอร์

express.json() -> แปลง JSON string เป็น JavaScript Object ยัดใส่ req.body

Route ส่งมาตามคำขอ ที่ยิงมาเช่น POST /products มันวิ่งเข้าหาโค้ด app.post /products

Code Logic -> ตรวจความครบถ้วน -> บันทึกลงฐานข้อมูล Database

Server Response -> ส่ง res.status(201) พร้อมข้อมูลสินค้ากลับไป -> จบงาน

---

**8. CRUD คืออะไร? จับคู่แต่ละ operation กับ HTTP method และ route ที่คุณใช้ใน API**

CRUDย่อมาจาก Create,Read,Update, และ Delete เป็น 4 ฟังก์ชันพื้นฐานในการจัดการข้อมูลในระบบฐานข้อมูลทุกประเภทครับ

Create การสร้างข้อมูลใหม่ POST การทำงาน: Client ส่งข้อมูลสินค้าชิ้นใหม่ในรูปแบบ JSON ผ่าน Request Body มาที่ Server เพื่อนำไปเพิ่มลงฐานข้อมูล

Read การดึงหรือดูข้อมูล GET การทำงาน: Server ดึงข้อมูลจากฐานข้อมูลออกมาโชว์ให้ Client ดู โดยไม่มีการแก้ไขข้อมูลใดๆ

Update การแก้ไขข้อมูลที่มีอยู่แล้ว PUT หรือ PATCH การทำงาน: Client ระบุ ID ของสินค้าที่ต้องการแก้ไขผ่าน Params แล้วส่งข้อมูลชุดใหม่มาทาง Bodyถ้าใช้ PUT จะเป็นการเปลี่ยนแทนที่ข้อมูลชิ้นนั้นใหม่ทั้งหมดถ้าใช้ PATCH จะเป็นการเลือกแก้ไขข้อมูลแค่บางฟิลด์ (เช่น แก้ไขเฉพาะราคา)

Delete การลบข้อมูล DELETE การทำงาน: Client ระบุ ID ของสินค้าที่ต้องการลบผ่าน Params เมื่อ Server ได้รับคำสั่งก็จะทำการลบข้อมูลชิ้นนั้นออกจากฐานข้อมูลทันที

---

**9. API ของคุณตอบสนองอย่างไรเมื่อมีอะไรผิดพลาด — เช่น เมื่อ product ตาม ID ที่ระบุไม่มีอยู่จริง?**

เมื่อเกิดข้อผิดพลาดขึ้นในระบบ API ของเรา เช่น ในกรณีที่ Client ส่ง Request มาเพื่อขอเข้าถึง แก้ไข หรือลบสินค้า แต่พบว่า ID สินค้านั้นไม่มีอยู่จริงในฐานข้อมูล API จะตอบกลับตามขั้นตอนมาตรฐาน ด้วยรหัส 404 ทันที เพื่อบอกให้ฝั่ง Client รู้ว่า ไม่มีข้อมูลอยู่จริงในระบบ

---

## Frontend & Integration

**10. CORS คืออะไร และแก้ปัญหาอะไร? ถ้าไม่ได้ config ไว้บน server ของคุณ คุณจะเห็นอะไรใน browser?**

ผมไปหาข้อมูลมาว่าเป็นการข้ามข้อจำกัดของ browser ที่ไม่ให้เราส่ง api ข้าม port ผมไม่ทันได้เจอปัญหาเพราะผมติดตั้ง cors และเรียกใช้ฟังชัน cors()มาก่อนจะเชื่อมต่อ api 

---

**11. แอป React ของคุณ fetch ข้อมูลจาก API ที่ไหน? อธิบายว่า `useEffect` ในโค้ดนั้นทำอะไร และทำไมถึงเรียก fetch ตรง ๆ ใน component body ไม่ได้**

fetch ข้อมูลจาก api ที่ http://localhost:3001/products 
ส่วน useEffect ผมใช้จัดการข้อมูลที่ fetch มา แล้วส่งค่าไปแสดงผล

ผมไม่แน่ใจว่าทำไมถึงเรียก fetch ตรง ๆ ใน component body ไม่ได้แต่คิดว่าผลออกมาไม่ดีแน่ๆ

---

**12. API base URL ของคุณถูกกำหนดไว้ที่ไหน และทำไมถึงเลือกเก็บไว้ตรงนั้น แทนที่จะ hardcode ไว้ในทุก fetch call?**

เก็บใน.env ใส้ตัวแปรไว้ เพราะเวลาเปลี่ยน port จะได้ไม่เหนื่อย

---

**13. เลือก action หนึ่งในแอปของคุณ — เช่น การลบ product อธิบายการเดินทางแบบครบวงจร (full round trip): เกิดอะไรขึ้นตั้งแต่ผู้ใช้คลิกปุ่ม ไปจนถึง request ไปถึง server จนถึงหน้าจออัปเดตด้วย list ใหม่**

การลบ product
1.กดปุ่ม delete
2.ปุ่มส่ง id ที่ฟังชัน handleDelete(id)
3.fetch delete ไปที่ http://localhost:3001/products/${id} 
4. server ทำ logic ลบสินค้าออกจาก array
5. react จัดการ state แล้ว render รายชื่อสินค้าใหม่

---

**14. แอปของคุณแสดงอะไรให้ผู้ใช้เห็นระหว่างที่ข้อมูลกำลังโหลด และแสดงอะไรถ้า fetch ล้มเหลว (เช่น server ไม่ได้รันอยู่)? ทำไมเรื่องนี้ถึงสำคัญ?**

state loading และ error ไว้แสดง status อยู๋ สำคัญเพราะสิ่งต่างๆมันต้องคาดเดาได้

---

**15. หลังจากที่คุณ add, edit, หรือ delete product แล้ว list บนหน้าจอของคุณอัปเดตโดยไม่ต้อง refresh หน้าเว็บ อธิบายว่าทำไมถึงเป็นแบบนั้น — อะไรที่ทำให้ React re-render ด้วยข้อมูลใหม่?**

ทุกครั้งที่ state มีการเปลี่ยนแปลง ตัวreact จะre-render อัตโนมัติ จากฟังชี่น ,set.. ที่มีการเปลี่ยนแปลงค่า

---

**16. ส่วนไหนที่ยากที่สุดในการเชื่อมแอป React ของคุณเข้ากับ Express API และคุณทำอย่างไรถึงผ่านมันมาได้?**

เรื่องลำดับของ Middleware และข้อมูลที่กลายเป็น undefined

ส่วนที่ยากที่สุด ช่วงแรกเจอปัญหาว่าเมื่อส่งคำสั่ง POST มาที่ Server ข้อมูลใน req.body จะกลายเป็น undefined และทำให้ระบบพัง Crash ตลอดเวลา ทั้งๆ ที่ตรวจสอบฝั่งหน้าบ้านแล้วว่าส่งข้อมูล JSON มาอย่างถูกต้อง ทำให้เสียเวลาไล่หาบั๊กอยู่นาน

วิธีผ่านมันมาได้ ผมใช้วิธีค้นคว้าข้อมูลและทำความเข้าใจกลไกการทำงานของ Express.js จนพบว่า Express ทำงานเป็นลำดับขั้นเหมือนสายพาน บั๊กเกิดจากผมเขียนแนวเส้นทาง Routes ไว้ ก่อนหน้า บรรทัด app.use(express.json()) ทำให้เซิร์ฟเวอร์ดึงข้อมูลไปใช้ก่อนที่จะถูกแปลงภาษา วิธีแก้คือผมจึงจัดระเบียบโครงสร้างโค้ดใหม่ โดยย้าย Middleware ตัวแปลงข้อมูลและระบบความปลอดภัยไปไว้ที่ บนสุดของไฟล์เสมอ เหตุการณ์นี้ทำให้ผมเข้าใจเรื่อง Middleware Lifecycle มากยิ่งขึ้นครับ

---

## AI Process

ตอบส่วนนี้เฉพาะถ้าคุณ rate ตัวเอง **2 ขึ้นไป** บน AI Code Contribution Scale ด้านบน ถ้า rate ไว้ 0 หรือ 1 ให้เขียน "N/A" ใต้แต่ละคำถาม

**17. ถ้าคุณใช้ AI สร้างโค้ด คุณแบ่งงานออกเป็นขั้นตอนหรือ prompt อย่างไร? ยกตัวอย่าง prompt จริงที่คุณใช้ 1 อัน แทนที่จะเป็น prompt เดียวแบบ "สร้างทั้งแอปให้หน่อย"**

ใช้ถาม และถามสาเหตุ error ไม่ได้ให้มันเขียนcode หรือ debugให้

---

**18. อธิบายสิ่งที่ AI tool สร้างให้ 1 อย่างที่คุณเปลี่ยน แก้ไข หรือปฏิเสธ — พร้อมเหตุผลว่าทำไม**

ไม่ได้ใช้สร้างอะไร

---

**19. อธิบาย bug หรือ error จริง ๆ ที่คุณเจอระหว่าง build โปรเจกต์นี้ 1 อย่าง คุณหาสาเหตุที่แท้จริงได้อย่างไร นอกเหนือจากการ copy error ไปถามใน chat?**

path ไม่ตรง และ typo error ดึง variableไม่ตรง ที่ประกาศ

---

**20. เลือก route (backend) หรือ component (frontend) 1 อันที่ AI ช่วยสร้าง โดยไม่ย้อนกลับไปดู AI chat history อธิบายว่ามันทำอะไรและทำไมถึงทำงาน ด้วยคำพูดของคุณเอง**

ช่วงเชื่อม frontend กับ api ใช้ AI ในการไกด์ลำดับขั้นตอนที่จะทำ
